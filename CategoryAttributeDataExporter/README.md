# Overview
Module adds custom (user-defined) EAV attribute support for categories to the Data Exporter.

- add `attributes` field on the categories feed
- add `categoryAttributes` feed - category-attribute metadata feed (attribute code, label, data type, per store/website/store view)
